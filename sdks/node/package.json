{
  "name": "cogos-client",
  "version": "0.1.0",
  "description": "Node client for cogos.5ceos.com — schema-locked LLM inference with cryptographic response signing",
  "license": "MIT",
  "author": "5CEOS Inc.",
  "homepage": "https://cogos.5ceos.com",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/",
    "README.md"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:test": "tsc -p tsconfig.test.json",
    "test": "npm run build:test && node --test --test-reporter=spec dist-test/test/*.test.js",
    "prepublishOnly": "npm run build && npm test"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
