#!/usr/bin/env bash
# scripts/admin-ceremony/list-keys.sh
#
# STUB. See scripts/admin-ceremony/README.md for the design intent.
#
# Final form: read the local signed config artifact and print the issued
# keys (hashes + tenant + tier + revoked-at, never plaintext). Read-only;
# does not produce a signed diff.

set -euo pipefail

echo "TODO: implement key listing from the signed config artifact. Tracking issue: <TBD>." >&2
exit 1
